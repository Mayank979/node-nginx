### Node Scaling with Docker and Nginx

## Steps:

## Pre-Requisite: Install docker and docker compose and start docker

#### docker compose up --build -d --scale nodeapp=${no. of docker containers to run}
